import { withMainLayout } from '~/layouts'

import Home from './Home'
import NotFound from './NotFound'

export const HomeScreen = withMainLayout(Home)
export const NotFoundScreen = withMainLayout(NotFound)
